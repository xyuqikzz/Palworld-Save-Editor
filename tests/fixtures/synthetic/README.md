# Synthetic fixtures

Synthetic dictionaries and codec fragments are used for boundary, rollback,
compatibility-alias, unknown-byte and failure-injection tests. They are never
reported as real Palworld save round trips and do not enable a production write
capability whose layout or rule still requires real-save evidence.
